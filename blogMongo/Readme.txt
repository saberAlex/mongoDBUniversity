Building an article knowledge based.
we use the meanSTACK..  (not the mean JS package...)
	> npm install express
	> npm install express-generator
	> express <app name>

create the model for each route... 
model should always be singular (typical convention)

>Front end... 
We need to show our public directory:
first to make our bower component work we need to use this:
	git config --global url."https://".insteadOf git://
